import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 32,
    overflow: "hidden",
    width: 32,
  },
  img: {
    height: "100%",
    width: "100%",
  },
});

export default styles;
